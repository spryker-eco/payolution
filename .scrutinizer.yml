build:
  dependencies:
    override:
      - true
  environment:
    php:
      version: 7.1
filter:
  excluded_paths:
    - build/*
    - tests/*
    - vendor/*
    - config/Shared/*
    - src/Generated/*
    - src/Orm/*
    - src/Pyz/*
checks:
  php:
    code_rating: true
